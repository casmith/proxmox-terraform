# Terraform files
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
terraform-*.log
# Variable files with secrets
# Note: secrets.tfvars is encrypted with SOPS and is safe to commit
# The age.key file contains the private key and must NOT be committed
age.key
secrets.*.tfvars
*.auto.tfvars

# Crash log files
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc
